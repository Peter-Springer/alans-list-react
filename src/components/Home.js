import React, { Component } from 'react';

class Home extends Component {
  render() {
    return (
      <div className="Home">
        <button>Post</button>
        <button>Browse</button>
      </div>
    );
  }
}

export default Home;
